import { SmartMenuDirective } from './smart-menu.directive';

describe('SmartMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new SmartMenuDirective();
    expect(directive).toBeTruthy();
  });
});
