import { GoogleRecaptchaDirective } from './google-recaptcha.directive';

describe('GoogleRecaptchaDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleRecaptchaDirective();
    expect(directive).toBeTruthy();
  });
});
